h1 Thăm ngàn

a(href='/todos/create') Create

form(action="/todos/search", method="GET")
  input(type="text", name='q', value=question)
  button Search
  
ul
  each work, index in works
    li= (index+1) +' : ' +work.name